// const path = require('path').join(__dirname, 'wasm_transformer_bg.wasm');
// const bytes = require('fs').readFileSync(path);
var bytes;
// iOS: load with with XhttpRequest: 
var bytes; 
var request = new XMLHttpRequest();
request.onload = function() {
	bytes = request.response;
}
request.open("GET", __dirname + 'wasm_transformer_bg.wasm', false); 
request.responseType = 'arraybuffer';
request.send();

let imports = {};
imports['./wasm_transformer.js'] = require('./wasm_transformer.js');

const wasmModule = new WebAssembly.Module(bytes);
const wasmInstance = new WebAssembly.Instance(wasmModule, imports);
module.exports = wasmInstance.exports;
